Create a concise implementable plan.
Each subtask must be a small vertical increment that leaves checks passing.
The criteria array contains ONLY exact IDs such as ["AC1","AC2"], never explanations.
Cover every AC ID; do not invent IDs.
Usually 1–3 subtasks; separate independently deliverable behaviors rather than code vs tests.
Reconcile research doubts against the actual specification and code.
Only identify a blocker if an unresolved product decision changes acceptance; choose reversible details yourself.
Missing implementation is the purpose of this workflow, NEVER a blocker.
Do not reconfirm behavior already stated in acceptance.
No code edits.
