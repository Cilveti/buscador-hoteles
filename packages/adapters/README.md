# Adaptadores deterministas

- `synthetic-stays`: búsqueda sobre un conjunto de estancias sintéticas, usada únicamente para enseñar reglas y tests. No representa disponibilidad ni precios reales.

La persistencia del catálogo con Payload vive en `apps/web/src/composition/catalog`.
